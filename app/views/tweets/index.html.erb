<% @tweets.each do |tweet| %>
<p>
    <%= tweet.message %>
    <%= image_tag "/get_image/#{tweet.id}" %>
    <%= link_to "いいね", likes_path(tweet_id: tweet.id),method: :post %>
    <%= link_to "いいね削除", like_path(tweet), methoe: :delete %>
    <%= link_to "削除", "/tweets/#{tweet.id}" , data:{turbo_method: :delete} %>
    <%= link_to '表示', "/tweets/#{tweet.id}" %>
</p>
    
<% end %>

<%= link_to "ツイートする", new_tweet_path %>